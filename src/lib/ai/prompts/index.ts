// This is a placeholder file for AI prompts.
// In a real project, this would contain various prompt definitions.
// For the purpose of this task, we are creating a dummy file to resolve the import error.

export const updateDocumentPrompt = (content: string, kind: string) => {
  return `You are an AI assistant that helps update documents. The current content is: ${content}. The document kind is: ${kind}. Please provide the updated content.`;
};
